

export function signinUser({ email, password }) {
  return function(dispatch) {
    // Submit email/apssword to server

    // If request is good...
    // - Update state to indicate user is authorized
    // - Save the JWT token
    // - redirect to the route '/feature'

    // If request is bad...
    // - Show an error to the user
  }
}
